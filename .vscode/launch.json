{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug",
            "type": "cppvsdbg",
            "request": "launch",
            "preLaunchTask": "Build",
            "program": "${workspaceRoot}/build/handmade-hero.exe",
            "cwd": "${workspaceRoot}",
        }
    ]
}