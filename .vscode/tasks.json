{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build",
            "type": "shell",
            "command": "odin build code -debug -out:build/handmade-hero.exe",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}